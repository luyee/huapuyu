package com.anders.vote.dao.impl;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.test.context.ContextConfiguration;import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;import com.anders.vote.bo.Poll;import com.anders.vote.dao.PollDao;@RunWith(SpringJUnit4ClassRunner.class)// @ContextConfiguration(locations = { "classpath:spring.xml", "classpath:spring-test.xml" })@ContextConfiguration(locations = "classpath:applicationContext.xml")public class PollDaoTest {	@Autowired	private PollDao pollDao;	@Test	public void testGetPollById() {		Poll poll = new Poll();		poll.setTitle("投票1");		poll.setRemark("投票测试1");		pollDao.save(poll);				poll = pollDao.getById(poll.getId());		poll.setRemark("投票测试2");		pollDao.update(poll);				poll.setId(99L);		pollDao.saveOrUpdate(poll);				pollDao.getAll();				pollDao.disabledById(poll.getId());				pollDao.findAll();				pollDao.deleteById(poll.getId());					}}