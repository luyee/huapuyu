<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anders.ssh.dao.mybatis.DataMapper">

	<cache />
	
	<resultMap id="resultMap" type="com.anders.ssh.bo.xml.Data">
	    <id column="id" property="id" jdbcType="BIGINT" />
	    <result column="name" property="name" jdbcType="VARCHAR" />
	    <result column="type" property="type" jdbcType="TINYINT" />
    	<result column="enable" property="enable" jdbcType="BOOLEAN" />
	</resultMap>
  	<sql id="columnList" >
    	id, name, type, enable
  	</sql>
  	
  	<select id="getAll" resultType="User">
    	SELECT
    	<include refid="columnList" />
		FROM cfg_data
  	</select>

	<select id="getById" parameterType="Long" resultType="Data">
    	SELECT 
    	<include refid="columnList" /> 
    	FROM cfg_data 
    	WHERE id = #{id}
	</select>
  
  	<insert id="save" parameterType="Data">
    	INSERT INTO cfg_data
      		(id, name, type, enable)
    	VALUES
      		(#{id}, #{name}, #{type}, #{enable})
  	</insert>
  
  	<update id="update" parameterType="Data">
    	UPDATE cfg_data SET
     		name = #{name},
      		type = #{type}
    	WHERE 
    		id = #{id}
  	</update>
  
  	<delete id="deleteById" parameterType="Long">
		DELETE FROM cfg_data WHERE id = #{id}
  	</delete>
  	
  	<delete id="delete" parameterType="Data">
		DELETE FROM cfg_data WHERE id = #{id}
  	</delete>
</mapper>