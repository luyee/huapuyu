unit shh;

interface

uses Windows, Classes, Graphics, Forms, Controls, StdCtrls, Dialogs, ComCtrls;

type
  TFrmShhNew = class(TForm)
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

implementation

{$R *.dfm}

procedure TFrmShhNew.FormClose(Sender: TObject; var Action: TCloseAction);
begin
  Action := caFree;
end;

end.
